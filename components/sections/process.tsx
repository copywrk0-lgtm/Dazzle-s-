import { Container } from "../ui/container";
import { SectionHeading } from "../ui/section-heading";
import { Reveal } from "../ui/reveal";
export function Process(){const steps=[["01","Tell us about the car","Share the model, condition and what you want protected."],["02","Choose the plan","We recommend the right route for the vehicle."],["03","Precision installation","Your vehicle is prepared, protected and inspected."],["04","Handover","We walk you through the finished work and care instructions."]];return <section className="py-28 md:py-36"><Container><SectionHeading eyebrow="The process" title="From enquiry to handover." body="The site removes uncertainty before the customer arrives: what to choose, what happens and how to start."/><div className="grid gap-5 md:grid-cols-4">{steps.map(s=><Reveal key={s[0]}><div className="border-t border-white/10 pt-5"><div className="text-xs font-black text-champagne">{s[0]}</div><h3 className="mt-8 text-xl font-semibold">{s[1]}</h3><p className="mt-3 text-sm leading-6 text-white/40">{s[2]}</p></div></Reveal>)}</div></Container></section>}